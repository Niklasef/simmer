[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "simmer"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pynput", "Pillow"]

[project.scripts]
simmer = "simmer.cli:main"
area-mapper = "simmer.area_mapper:main"
